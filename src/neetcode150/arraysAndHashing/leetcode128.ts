function longestConsecutive(nums: number[]): number {
  // this problem is honestly a graph one where we can utilize find and find connected components checking the length of each connected component
}

export default longestConsecutive;
