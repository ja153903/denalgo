// TODO: Solve this problem later
function findDuplicate(nums: number[]): number {
  // this problem is not explicitly like a linked list problem
  // but we can adapt fast and slow pointers to solve this
  // nums = [1,3,4,2,2]

  return -1;
}

export default findDuplicate;
