import {
  assert,
  assertEquals,
  assertObjectMatch,
  assertStringIncludes,
} from "https://deno.land/std@0.131.0/testing/asserts.ts";

export { assert, assertEquals, assertObjectMatch, assertStringIncludes };
